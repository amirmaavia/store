import React from 'react';
interface BulletProps {
    BulletComponent?: BulletComponentType;
    bulletStyle?: BulletStyle;
    index: number;
    setSlide: (index: number) => void;
    activeIndex: number;
}
export declare type BulletComponentType = (props: BulletComponentProps) => React.ReactElement;
export interface BulletStyle {
    [key: string]: string;
}
interface BulletComponentProps {
    isActive: boolean;
    onClick: () => void;
}
declare const Bullet: React.FunctionComponent<BulletProps>;
export default Bullet;
